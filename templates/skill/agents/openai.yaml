interface:
  display_name: "Skill Template"
  short_description: "Starter metadata for a Codex skill"
  default_prompt: "Use $skill-template to perform the reusable workflow."

policy:
  allow_implicit_invocation: false
